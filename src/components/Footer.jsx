import React from 'react';
import '../styles/footer.css';

const Footer = () => {
    return (
        <footer className="footer">
            <p className="footer-text">Internal Use Only • Secure Connection Required</p>
        </footer>
    );
};

export default Footer;